export default function Body() {
  return (
    <p className={'mt-4 max-w-prose text-left font-open-sans tracking-wide text-white'}>
      This project includes my PyGame projects, ranging from Button Demo, Aerial Evader, and Py-Man,
      whose respective data structures and algorithms have varying time complexities between linear,
      <code>O(n)</code>, and constant, <code>O(1)</code> times, respectively.
    </p>
  );
}
